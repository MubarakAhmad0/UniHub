apiVersion: v1
kind: Service
metadata:
  name: boilerplate-service
  namespace: default
spec:
  type: NodePort
  ports:
    - port: 3001
      targetPort: 3000
      protocol: TCP
  selector:
    app: boilerplate